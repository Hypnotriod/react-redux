export const API_HOST: string = 'http://localhost:3000';
export const API_LOGIN: string = `${API_HOST}/auth/login`;
export const API_LOGOUT: string = `${API_HOST}/auth/logout`;
export const API_REFRESH_TOKEN: string = `${API_HOST}/auth/refresh`;

export const LOGIN_MIN_LENGTH: number = 8;
export const LOGIN_MAX_LENGTH: number = 32;
